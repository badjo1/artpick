<div class="continuum-container">
  <header style="border-bottom: 1px solid var(--color-border); padding-bottom: var(--spacing-md); margin-bottom: var(--spacing-lg);">
    <h1><%= @exhibition.title %> - All Artworks</h1>
    <p style="color: var(--color-text-secondary);">
      <%= link_to "← Back to Exhibition", exhibition_path(@exhibition) %>
    </p>
  </header>

  <% if @artworks.any? %>
    <div class="artwork-grid">
      <% @artworks.each_with_index do |artwork, index| %>
        <%= render partial: "artworks/card", locals: { artwork: artwork, rank: index + 1 } %>
      <% end %>
    </div>
  <% else %>
    <p style="text-align: center; color: var(--color-text-secondary); padding: 4rem;">
      No artworks in this exhibition yet.
    </p>
  <% end %>
</div>
